function merge(arrA, arrB) {
  let elements = arrA.length + arrB.length;
  let mergedArr = new Array(elements).fill(0);
  return mergedArr;
}

function mergeSort(arr) {
  return arr;
}

module.exports = {
  mergeSort
};
