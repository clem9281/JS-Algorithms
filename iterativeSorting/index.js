function selectionSort(arr) {
  for (let i = 0; i < arr.length - 1; i++) {
    let curIndex = i;
    let smallestIndex = curIndex;
    // TODO: find next smallest element

    // TODO: swap
  }
  return arr;
}
function insertionSort(arr) {
  return arr;
}

function bubbleSort(arr) {
  return arr;
}

function countSort(arr, maximum = -1) {
  return arr;
}

module.exports = { selectionSort, bubbleSort, countSort, insertionSort };
